# ──────────────────────────────────────────────────────────────
# Personal Memory Assistant – Environment Configuration
# ──────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# All variables are prefixed with PMA_ when read by the app.

# ── Server ────────────────────────────────────────────────────
# PMA_HOST=127.0.0.1
# PMA_PORT=8000

# ── LLM Provider ─────────────────────────────────────────────
# Gemini (recommended): get a key at https://aistudio.google.com/
PMA_GEMINI_API_KEY=
# PMA_GEMINI_MODEL=gemini-flash-latest
# PMA_GEMINI_TIMEOUT=30

# Ollama fallback (used when no Gemini key is set)
# PMA_OLLAMA_URL=http://localhost:11434/api/generate
# PMA_OLLAMA_MODEL=llama3
# PMA_OLLAMA_TIMEOUT=60

# ── Embeddings ────────────────────────────────────────────────
# PMA_EMBEDDING_MODEL=all-MiniLM-L6-v2
# PMA_EMBEDDING_BATCH_SIZE=64

# ── Database ──────────────────────────────────────────────────
# PMA_DB_PATH=pma_metadata.db
# PMA_CHROMA_PERSIST_DIR=chroma_db

# ── Indexing ──────────────────────────────────────────────────
# PMA_CHUNK_SIZE=512
# PMA_CHUNK_OVERLAP=50
# PMA_MAX_FILE_SIZE_MB=50
# PMA_INDEX_CONCURRENCY=4
# PMA_SUPPORTED_EXTENSIONS=.txt,.md,.pdf,.docx,.csv,.json,.py,.js,.ts,.java,.c,.cpp,.rs,.go,.rb,.html,.css,.xml,.yaml,.yml,.toml,.ini,.cfg,.sh,.bat

# ── RAG / Retrieval ──────────────────────────────────────────
# PMA_RRF_FTS_WEIGHT=0.4
# PMA_RRF_SEMANTIC_WEIGHT=0.6
# PMA_RETRIEVAL_TOP_K=10
# PMA_CONTEXT_MAX_TOKENS=4000

# ── Dev / Feature Flags ──────────────────────────────────────
# PMA_DEV_MODE=true
# PMA_LOG_LEVEL=INFO
